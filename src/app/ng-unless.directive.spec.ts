import { NgUnlessDirective } from './ng-unless.directive';

describe('NgUnlessDirective', () => {
  it('should create an instance', () => {
    const directive = new NgUnlessDirective();
    expect(directive).toBeTruthy();
  });
});
