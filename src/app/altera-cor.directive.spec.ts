import { AlteraCorDirective } from './altera-cor.directive';

describe('AlteraCorDirective', () => {
  it('should create an instance', () => {
    const directive = new AlteraCorDirective();
    expect(directive).toBeTruthy();
  });
});
